[options]
# Package discovery is configured in pyproject.toml
# ([tool.setuptools.packages.find]), which needs two roots ("." and "manifold")
# now that manifold_gp lives under manifold/. Declaring `packages = find:` here
# as well would override that with a single-root search and drop manifold_gp.
